# ── Local development (no Hopsworks, uses local Parquet storage) ───────────────
# Use this on Windows to avoid C++ build tool errors.
# All data is stored in data/aqi_features.parquet automatically.
# Switch to requirements.txt when deploying to Linux with Hopsworks.

pandas>=2.1.0
numpy>=1.24.0
requests>=2.28.0
python-dotenv>=1.0.0
tenacity>=8.0.0
pyarrow>=12.0.0
joblib>=1.2.0

scikit-learn>=1.3.0
tensorflow-cpu>=2.13.0
shap>=0.42.0

mlflow>=2.8.0

streamlit>=1.28.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
plotly>=5.15.0
matplotlib>=3.7.0

scipy>=1.10.0
